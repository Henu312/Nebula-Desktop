# Nebula Desktop 性能验证记录

## 1. 测试范围

任务：`T6.1 性能验证`

测试时间：2026-05-28

系统：Windows 11 Home 25H2

验收标准：

- 启动时间 `< 2s`
- 空闲 CPU `< 1%`
- 内存 `< 200MB`
- 窗口枚举频率合理，避免高频轮询

## 2. 测试方法

启动产物：

```text
D:\data\Nebula_Desktop\src-tauri\target\debug\nebula-desktop.exe
D:\data\Nebula_Desktop\src-tauri\target\release\nebula-desktop.exe
```

采样方式：

1. 使用 `Start-Process` 启动 Nebula Desktop。
2. 从启动计时到进程出现非 0 `MainWindowHandle`，记录为启动时间。
3. 启动后等待 3 秒进入空闲状态。
4. 采样 5 秒 CPU 时间差，按逻辑处理器数量折算 CPU 百分比。
5. 记录 Working Set、Private Memory、线程数、句柄数。
6. 测试后关闭本轮启动的 Nebula 进程。

窗口枚举频率：

```ts
WINDOW_REFRESH_INTERVAL_MS = 2_500
```

结论：

- 当前窗口状态刷新间隔为 2.5 秒，不属于高频轮询。

## 3. Debug 产物采样

| 指标 | 结果 |
|---|---:|
| 启动时间 | 2461ms |
| 空闲 CPU | 0.017% |
| Working Set | 44.03MB |
| Private Memory | 10.36MB |
| 线程数 | 12 |
| 句柄数 | 427 |

结论：

- CPU 达标。
- 内存达标。
- Debug 启动时间超过 2 秒，不作为最终性能验收依据。

## 4. Release 产物采样

构建命令：

```powershell
cargo tauri build --no-bundle
```

产物：

```text
D:\data\Nebula_Desktop\src-tauri\target\release\nebula-desktop.exe
```

### 4.1 构建后首次冷启动

| 指标 | 结果 |
|---|---:|
| 启动时间 | 2276ms |
| 空闲 CPU | 0.035% |
| Working Set | 39.47MB |
| Private Memory | 10.25MB |
| 线程数 | 12 |
| 句柄数 | 421 |

结论：

- CPU 达标。
- 内存达标。
- 构建后首次冷启动略高于 2 秒，需要后续复测和优化。

### 4.2 Release 连续热启动

| 轮次 | 启动时间 | 空闲 CPU | Working Set | Private Memory | 线程数 | 句柄数 |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 155ms | 0.020% | 39.05MB | 10.20MB | 12 | 419 |
| 2 | 79ms | 0.000% | 38.88MB | 10.27MB | 12 | 418 |

结论：

- 常规 release 热启动达标。
- 空闲 CPU 达标。
- 内存达标。

## 5. 总结

当前状态：

- 空闲 CPU：通过。
- 内存占用：通过。
- 窗口枚举频率：通过。
- Release 热启动：通过。
- 构建后首次冷启动：未完全达标，需复测。

后续建议：

1. 在非构建后环境中复测 release 冷启动。
2. 将首屏渲染与 IPC 数据加载进一步解耦，避免首屏等待系统状态、存储、窗口枚举和 notification indicator。
3. 如果冷启动仍超过 2 秒，优先优化前端首屏初始化和 Tauri setup 阶段。
